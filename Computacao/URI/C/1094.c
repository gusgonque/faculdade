#include <stdio.h>

int main () {
  int num_casos, num, num_r, num_c, num_s, tot_animais, tot_r, tot_c, tot_s, char;
  scanf("%d", &num_casos);
  for (  ; num_casos > 0 ; num_casos-- ) {
    scanf("%d %c", num, char);
    if ()
  }

  return 0;
}
