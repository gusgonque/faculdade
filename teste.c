#include <stdio.h>

void Ordem_Crescente (int a,)

int main () {
  int x, y, z;
  while (1) {
    scanf("%d %d %d", &x, &y, &z);
    Ordem_Crescente (x,y,z);
    Ordem_Decrescente (x,y,z);
  }
  return 0;
}
